@model StoreFront.Data.EF.Position

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Position</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Position1)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Position1)
        </dd>

    </dl>
</div>
<p>
    @if (User.IsInRole("Admin"))
    {
        @: @Html.ActionLink("Edit", "Edit", new { id = Model.PositionID }) |
    }

    @Html.ActionLink("Back to List", "Index")
</p>
